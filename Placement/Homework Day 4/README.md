# Homework — Day 4 (Conditionals)

This folder contains homework exercises assigned during Day 4 of the placement training. The exercises focus on conditional statements and comparison logic in Python.

How to run

- Use Python 3.x.
- From the repository root, run a script with:

  ```bash
  python "Placement/Homework Day 4/<script_name>.py"
  ```

Files and purpose

- biggest_of_two.py
  - Purpose: Determine and print the larger of two numbers (or report if they are equal).
  - Status: placeholder (empty file). I can add a concise implementation if you want.
  - Example usage (after implementation):
    ```bash
    python "Placement/Homework Day 4/biggest_of_two.py"
    # Enter two numbers when prompted, or modify the script to accept args
    ```

- divisible_by_5_and_11.py
  - Purpose: Check whether a given number is divisible by 5, by 11, by both, or by neither, and print the appropriate message.
  - Example usage:
    ```bash
    python "Placement/Homework Day 4/divisible_by_5_and_11.py"
    # The script may prompt for an integer input
    ```

- homework.py
  - Purpose: Placeholder for an aggregated homework runner or test harness for the exercises in this folder.
  - Status: placeholder (empty file). I can implement a small CLI that runs each script and shows sample inputs/outputs.

- largest_of_three_num.py
  - Purpose: Determine and print the largest of three numbers using conditional logic.
  - Example usage:
    ```bash
    python "Placement/Homework Day 4/largest_of_three_num.py"
    ```

- placement_eligiblity.py
  - Purpose: Example script that evaluates placement eligibility based on criteria such as marks, attendance, or other thresholds. (Filename contains a small typo — `placement_eligiblity.py`.)
  - Example usage:
    ```bash
    python "Placement/Homework Day 4/placement_eligiblity.py"
    ```

Notes and suggestions

- Two files are currently placeholders: `biggest_of_two.py` and `homework.py`. If you'd like, I can populate them with compact, well-documented implementations:
  - `biggest_of_two.py` — prompts for two numbers (or accepts CLI args) and prints the larger or reports equality.
  - `homework.py` — a small test runner that shows example runs of the other scripts (useful for instructors or automated checks).

- Would you like me to:
  1) Implement the placeholder scripts now and commit them, or
  2) Only update README further (formatting, examples, or add badges), or
  3) Leave as-is?

Tell me which option you prefer and I will update the repository accordingly.